{
  "name": "firefox-open-tab-on-the-right",
  "version": "2.3.0",
  "description": "Firefox and Chrome extension that opens a new tab to the right of the active tab, with customizable shortcuts and tab-moving commands.",
  "private": true,
  "scripts": {
    "test": "node --test",
    "test:e2e": "node --test e2e/extension.e2e.mjs",
    "test:e2e:chrome": "node --test e2e/chrome.e2e.mjs",
    "test:e2e:all": "npm run test:e2e && npm run test:e2e:chrome",
    "lint": "web-ext lint",
    "build": "web-ext build",
    "build:firefox": "node build.mjs firefox",
    "build:chrome": "node build.mjs chrome",
    "build:all": "node build.mjs all"
  },
  "devDependencies": {
    "geckodriver": "5.0.0",
    "puppeteer": "^25.2.1",
    "selenium-webdriver": "4.36.0",
    "web-ext": "^10.4.0"
  },
  "license": "MIT"
}
